import UIKit

// Нарушает принцип единственности, updateui нужно вынести в отдельный класс
class NetworkManager {
    func fetchData(url: URL) {
        // Запрос к API
    }

}

class UIManager {
    func updateUI() {
        // обновляет пользовательский интерфейс
    }
}
